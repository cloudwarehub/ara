import { parseIps } from 'ara/helpers/parse-ips';
import { module, test } from 'qunit';

module('Unit | Helper | parse ips');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseIps([42]);
  assert.ok(result);
});
