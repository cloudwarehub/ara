import { apiHost } from 'ara/helpers/api-host';
import { module, test } from 'qunit';

module('Unit | Helper | api host');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = apiHost([42]);
  assert.ok(result);
});
