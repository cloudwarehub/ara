import { versionCloudware } from 'ara/helpers/version-cloudware';
import { module, test } from 'qunit';

module('Unit | Helper | version cloudware');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = versionCloudware([42]);
  assert.ok(result);
});
