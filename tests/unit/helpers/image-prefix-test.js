import { imagePrefix } from 'ara/helpers/image-prefix';
import { module, test } from 'qunit';

module('Unit | Helper | image prefix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imagePrefix([42]);
  assert.ok(result);
});
